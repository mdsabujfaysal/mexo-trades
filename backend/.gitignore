# Dependencies
node_modules/

# Environment variables (contains real secrets — never commit)
.env

# Uploaded payment screenshots (user data — never commit)
uploads/*
!uploads/.gitkeep

# Logs
*.log
npm-debug.log*

# OS files
.DS_Store
Thumbs.db
